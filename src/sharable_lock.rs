pub struct SharableMutex<const BIT_OFFSET: usize, const USABLE_BITS: usize> {

}

impl<const BIT_OFFSET: usize, const USABLE_BITS: usize> SharableMutex<BIT_OFFSET, USABLE_BITS> {

    pub fn new() -> Self {
        Self {
            
        }
    }

}